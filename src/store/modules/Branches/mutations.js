// export const myMutations = (state)=>{

// }

export const setBranches = (state, branches) => {
  state.branches = branches;
};

export const setIsLoadingBranches = (state, val) => {
  state.isLoadingBranches = val;
};
